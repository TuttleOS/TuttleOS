export default function OwnerPage() {
  return (
    <section className="rounded-panel border border-grid bg-surface p-6 shadow-soft">
      <p className="text-[11px] font-bold uppercase tracking-wide text-accent-dk">
        Phase 1 foundation
      </p>
      <h1 className="mt-1 text-2xl font-bold">Owner dashboard</h1>
      <p className="mt-2 max-w-2xl text-muted">
        Phase 5 will wire stalled cases, SOL watch, approvals, conflicts, and
        override patterns from mockups/owner-dashboard-mockup.html.
      </p>
    </section>
  );
}
